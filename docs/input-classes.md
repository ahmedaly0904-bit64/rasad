# فئات المدخلات — `summarize()`

**المعيار:** قائمتان في الفئة نفسها إذا سلكتا **المسار نفسه** داخل `summarize`،
لا إذا تشابهت أرقامهما. `[1,2,3]` و`[100,200,300]` في فئة واحدة رغم اختلاف كل قيمة فيهما،
لأن الفروع التي تمرّان بها واحدة.

## الفئات

| # | الفئة | عيّنات | السلوك | التغطية |
|---|---|---|---|---|
| ١ | أقل من قيمتين | `[7.0]` · `[]` | `ValueError` | `test_requires_at_least_two_values` |
| ٢ | القيم كلها متساوية | `[2,2]` · `[0,0,0,0]` | `std = 0` → `cv = 0` | `test_constant_values_have_zero_spread` |
| ٣ | المتوسط صفر والقيم متغيّرة | `[-5,5]` · `[-3,3,0]` | `cv = inf` | `test_cv_is_infinite_when_mean_is_zero` |
| ٤ | تحتوي قيمة غير منتهية | `[1,inf]` · `[0.5,nan]` | `ValueError` | `test_rejects_non_finite_values` |
| ٥ | الحالة العادية | `[1,2,3]` · `[100,200,300]` | حساب كامل | `test_std_estimates_known_sigma` |

## الفئة الثانية تبتلع الثالثة

`[0,0,0,0]` متوسطها صفر، فظاهرها يضعها في الفئة الثالثة. غير أن `std == 0` تُفحص
**قبل** `mean == 0`، فتدخل الفئة الثانية. ترتيب الفروع هو الحاسم، لا شكل المُدخل.

ولو انعكس الترتيب لصُنِّف أثبت مخرَج ممكن — صفر ثابت — على أنه «تباين عالٍ».
الاختبار الحارس لهذا: `test_cv_is_zero_when_the_value_never_varies_at_zero`.

## الفئة الرابعة لم يكن لها فرع

قائمة تحتوي `nan` كانت تمرّ بلا اعتراض وتُرجع قاموسًا كل قيمه `nan`، ثم تصل تلك الـ `nan`
إلى `classify` حيث تُرجع كل مقارنة معها `False`، فتسقط إلى الفرع الأخير وتخرج بحكم `"high"`.
تشغيلة فاسدة تتحوّل إلى حكم واثق.

وُضع الفحص في `summarize` لا في `classify`، ليُمسك المُدخل عند حدّ النظام قبل أن ينتشر
في المفاتيح الثمانية التي تُرجعها الدالة.

## ما يزال مفتوحًا

`classify(float("nan"))` ما زالت تُرجع `"high"`. رفضت `summarize` المُدخل الفاسد،
لكن `classify` دالة عامة يمكن استدعاؤها وحدها، ولا حماية فيها بعد.
